services:
  - type: web
    name: family-schedule-planner-demo
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn schedule_app:app
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
    plan: free
